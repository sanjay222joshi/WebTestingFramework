/**
 * <p>This package contains the complex actions we need to execute
 * with the PageObjects.</p>
 *
 * <p>All the Action classes have a single execute method,
 * which return either a single Data object or a Collection of
 * Data objects.</p>
 *
 * <p>Currently only BrandPageData objects are supported.</p>
 */
package com.famous_smoke.automation.actions;